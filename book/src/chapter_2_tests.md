# Tests!

We should write some tests. Create a `tests` module in your crate like so:

```
#[cfg(test)]
    mod tests {
        #[test] 
        fn it_works() {
            // It works!
        }
    }
```
